# SQM Phase B2 PATCH_ONLY v6.3.7B2 — 검수센터(드래그+캡처+OCR+규칙저장)

생성일: 2026-03-05 14:22:29

## 포함 기능
- 원스톱 입고 팝업에 **🧪 검수센터** 버튼 추가 (파싱 완료 후 활성화)
- 검수센터(Review Center) 창:
  - PDF 보기(PyMuPDF) + 페이지 이동/줌
  - 드래그(ROI)로 텍스트 추출(텍스트 PDF)
  - 클립보드 이미지 붙여넣기(OCR) 버튼
    - pytesseract 우선 사용 (환경에 따라 tesseract 설치 필요)
  - 사용자가 정답 값 입력 후 **미리보기 데이터**에 즉시 반영
  - **규칙 저장**: SQLite 테이블 `review_rules` (engine.db 사용)

## 적용 방법
1. 현재 프로그램 폴더 백업
2. 압축 해제 후 동일 경로에 덮어쓰기
3. 원스톱 입고 → 파싱 완료 → 🧪 검수센터 클릭

## 주의
- OCR이 빈 문자열이면: tesseract 엔진 미설치일 수 있습니다.
- B3에서: (1) ROI OCR(페이지 일부만) (2) 규칙 자동 적용(재파싱 시) (3) Allocation/Picking 확장 진행 예정

# SQM — headless 테스트/디버깅에 필요한 최소 의존성
# 웹 세션(리눅스, GUI 없음)에서 pytest 를 돌리기 위한 패키지.
# 설치: pip install -r requirements-test.txt
#
# 주의: tkinter(GUI)는 서버에 없으므로 GUI 의존 테스트
#       (tests/test_inbound_doc_detector_artifact_guard.py)는 제외하고 실행한다.

pytest>=8.0

# 엔진/백엔드 테스트가 import 하는 런타임 패키지
pandas>=2.2
openpyxl>=3.1
fastapi>=0.110
uvicorn>=0.24
slowapi>=0.1.9
bcrypt>=4.0
python-multipart>=0.0.9
httpx>=0.27

# RELEASE NOTE — v6.2.3

배포일: 2026-02-27  
버전: `6.2.3`

## 핵심 변경

- 조회 메뉴/라벨 통일
  - `입고 현황 조회`, `출고 현황 조회` 메뉴 표기를 일관되게 정리
  - 출고 조회 팝업 제목도 동일 표기로 통일

- 조회 UI 통일 (입고/출고)
  - 출고 현황 조회를 입고와 동일한 기간 필터 패턴으로 변경
  - `시작일/종료일`, `이번 달/최근 7·30·90일/전체`, `조회` 버튼 제공
  - 컬럼명/요약 문구 톤 통일 (Title Case + `Total Weight`)
  - 하단 버튼 문구 통일: `📊 Excel 내보내기`, `❌ 닫기`

- 출고 조회 내보내기 기능 추가
  - 출고 현황 조회 결과를 Excel로 저장 가능
  - 조회 결과 존재 시에만 내보내기 버튼 활성화

- 추가 입고(2회차) UX 개선
  - 1차 업로드 완료 후 `예(추가 입고)` 선택 시 메인 재고 화면 초기화
  - 검색/기간/헤더필터/선택 LOT/하단 상세 패널 상태 자동 리셋

- 원스톱 파일 선택 속도 개선
  - 파일 1개 선택 후 다음 파일 선택 시 직전 폴더를 다시 열도록 개선
  - 드래그앤드롭 초기 파일이 있을 때도 해당 폴더를 시작 경로로 사용

## 운영 메모

- 릴리즈 커밋에서 실제 운영 DB 파일(`data/db/sqm_inventory.db`)과 임시 리뷰 폴더(`_tmp_crosscheck_review/`)는 제외했습니다.

# SQM 저장소 워킹트리 릴리즈 노트 (2026-04-04)

## 개요

`Sqm 재고관리` 단일 Git 저장소에 **여러 버전 디렉터리(v864 FULL, v865, v866, v867, v868)** 의 누적 변경·미추적 자산을 한 번에 반영한 동기화 커밋입니다. (이전 커밋에서 `Claude_SQM_v867` 단독 추가가 선행된 경우, 본 노트는 그 이후 **전체 워킹트리 정리**를 설명합니다.)

## 포함된 주요 트리

| 경로 | 내용 |
|------|------|
| `Claude_SQM_v864_20260329_FULL/` | 출고 리팩터 검증 스크립트·핸드오프·체크리스트, `.bkit` 감사/상태 갱신 |
| `Claude_SQM_v865/` | 엔진·GUI·탭·다이얼로그 수정, `react_api/`·`web/`·`tests/`·`scripts/`·`telegram_bot/` 등 |
| `Claude_SQM_v866/` | DB/마이그레이션·출입고·파서·GUI·문서(`Claude_Code_SQM_MASTER.md` 등), 웹/API/테스트 |
| `Claude_SQM_v868/` | v867 계열과 동일 구조의 워크스페이스 스냅샷(데스크톱+`react_api`+`web`+테스트) |

## 의도적으로 제외한 항목 (`.gitignore`)

- **`**/.claude/settings.local.json`** — 로컬 권한·환경 전용
- **`*.bak_auto`**, **`*.bak_20*`** — 편집기/작업 백업
- **`~$*`** — MS Office 임시 잠금 파일
- **`Claude_SQM_v8xx - 복사본/`** 등 — 수동 복사본(중복·대용량)
- 기존 규칙 유지: **`.env`**, **`*.db`**, **`node_modules/`**(각 `web/.gitignore`), **`logs/`**, **`output/`**, **`temp/`** 등

## Git 히스토리 (참고)

핵심 작업은 대략 아래 순서로 나뉩니다. 정확한 해시는 `git log --oneline` 으로 확인하세요.

1. **워킹트리 대량 동기화** — `v864` FULL·`v865`·`v866`·`v868` 미추적 자산 포함, 루트 `.gitignore` 보강, 본 릴리즈 노트 최초 추가.  
2. **`Claude_SQM_v868` 보정** — `react_api` 쓰기 경로·DB 유틸, `MASTER_FINAL_v868` 문서명 정리.  
3. **소규모 후속** — `.bkit` 감사 로그·릴리즈 노트 문구 등 (에이전트/IDE 사용 시 로그가 다시 바뀔 수 있음).

## 운영자 메모

- 버전별 **`version.py`** 가 다를 수 있습니다. 배포·빌드 시 **어느 폴더를 실행 기준으로 할지** 먼저 정하세요.
- 웹 프론트는 각 버전 `web/`에서 `npm install` 후 빌드/개발 서버를 사용합니다.
- Python 테스트는 해당 버전 폴더에서 `python -m pytest tests/ -q` 권장.

---

*(주) 지와이로지스 — SQM 재고관리 시스템*

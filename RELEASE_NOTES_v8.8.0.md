# SQM Inventory v8.8.0 — 디버깅 백로그 전수 조치 완료 (영역 A-D)

**릴리즈 날짜**: 2026-06-15
**브랜치**: `claude/debugging-session-optimization-t3ayma`
**이전 버전**: v8.7.9
**테스트 결과**: 325 passed, 1 deselected

---

## 🎯 주요 변경 사항

이번 릴리즈는 2026-06-15 진행된 **디버깅 백로그 전수 감사 결과(48개 항목)** 중 핵심 영역 A(시작/연결), B(입고/할당), C(출고/스캔), D(무결성/반품)에 대한 전수 조치를 포함합니다.

### 1. 영역 A: 앱 시작 및 연결성 개선 (A1~A9)
- **백엔드 연결 검증**: 스플래시 화면에서 `/api/health` 응답 검증을 강화하여 백엔드 시작 실패 시 흰 화면 고착 방지.
- **DB 마이그레이션**: 핵심 테이블 생성/마이그레이션 실패 시 앱 실행을 명시적으로 차단하고 로그 기록.
- **API 클라이언트**: 빈 응답(204) 및 비정상 응답에 대한 예외 처리 강화.
- **라우터 안정화**: 프론트엔드 라우터 초기화 실패 시 fail-safe 로직 적용.

### 2. 영역 B: 입고 및 할당 프로세스 강화 (B1~B12)
- **승인 반영 오류 수정**: `apply_approved` SQL 구문 오류 수정으로 승인 결과가 DB에 정상 반영되도록 보장.
- **PDF 입고 흐름**: 입고 문서 파싱 후 `PENDING` 상태에서 다음 단계 안내 및 가용재고 전환 유도.
- **부분 성공 명시**: 할당(Allocation) 시 일부 행 실패 사유를 분리하여 "전체 실패" 오인 방지.
- **AI 매핑 투명성**: Gemini API 키 미설정 등 AI 매핑 실패 사유를 사용자에게 명확히 고지.

### 3. 영역 C: 출고 및 바코드 스캔 최적화 (C1~C10)
- **감사 추적 보강**: `PICKED→SOLD` 전환 시 `stock_movement` 기록 누락 수정.
- **스캔 정합성**: 바코드 스캔 단계별 LOT 상태 재계산(`_recalc_lot_status`) 강제 호출로 UI-DB 동기화.
- **할당 계획 보호**: `stop_at_picked` 경로에서 `allocation_plan` 기록 보장.
- **트랜잭션 정리**: 스캔 엔진 내 중복 커밋 제거 및 트랜잭션 소유권 명확화.

### 4. 영역 D: 데이터 무결성 및 사후 처리 개선 (D1~D11)
- **반품 자동화**: 반품 처리 시 옵션에 따라 즉시 가용재고로 복귀하는 흐름 추가.
- **상태 복원 무게 보호**: `SOLD→PICKED` 등 상태 복원 시 톤백 데이터를 기반으로 헤더 무게 재계산.
- **정합성 체크 API**: `/api/integrity/check` 엔드포인트에 엔진의 정밀 검증(verify_all) 및 경고(warnings) 필드 연동.
- **원자성 보장**: 복수 LOT 입고 시 하나라도 실패하면 전체 롤백(All-or-Nothing).
- **하드스톱 처리**: 입고 후 즉각적인 정합성 위반 감지 시 입고 자체를 차단.

---

## 🛠 기술적 개선 및 유지보수
- **TDD 기반 개발**: 모든 버그 수정 및 기능 추가 시 전용 회귀 테스트(`tests/test_debug_goals_*.py`)를 선행 작성하여 검증.
- **안전한 SQL**: `rowcount` 검증을 통해 업데이트 실패 시 조용한 성공 방지.
- **로깅 표준화**: 예외 발생 시 `traceback`을 포함한 상세 로깅으로 유지보수성 향상.

---

## 🚦 테스트 리포트
- **전체 테스트**: `325 passed`, `1 deselected`
- **회귀 테스트**: 영역 A~D 전용 테스트 48종 포함.
- **무결성 조건**: 모든 테스트 케이스에서 `initial = current + picked` 불변식 성립 확인.

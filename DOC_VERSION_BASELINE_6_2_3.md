# 문서 버전 기준 정리 (v6.2.3)

## 목적

운영 기준 버전을 `6.2.3`으로 통일하기 위해, 문서 해석 기준을 아래처럼 고정합니다.

- **실행/배포 기준 버전:** `6.2.3`
- **코드 기준 버전 소스:** `version.py` 의 `__version__`
- **문서 내 과거 버전 표기:** 히스토리/릴리스 기록으로 간주 (운영 기준 아님)

## 운영 시 적용 규칙

1. 문서에 `SQM vX.Y.Z`가 있어도, 운영 판단은 항상 `version.py` 기준으로 합니다.
2. `RELEASE_NOTES_*`, `CHANGELOG_*`는 이력 문서이므로 과거 버전 문자열을 유지합니다.
3. 사용자 안내 문구/운영 체크리스트/신규 문서는 버전을 `v6.2.3`으로 표기합니다.

## 대상 문서 범위

아래 범위에서 `SQM v...` 표기가 확인되었습니다.

- `docs/` 전체 릴리스 노트/가이드/분석 문서
- 루트 문서: `README.md`, `QUICK_START.md`, `release_notes.md`, `RELEASE_NOTE_v6.1.9.md`, `RELEASE_NOTE_v6.2.0.md`, `AGENTS.md`, `CONTRIBUTING.md`

## 권장 사용 방법

- 운영자/현장 공지 시: `현재 운영 버전은 v6.2.3` 문구를 고정 사용
- 버전 확인 시: 앱 타이틀/로그가 아닌 `version.py`를 단일 소스로 확인

## 참고

- 본 문서는 **운영 기준 정리용**입니다.
- 과거 버전 문구를 문서 원문에서 일괄 치환하지 않고, 이력 보존을 우선합니다.
